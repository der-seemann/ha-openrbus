{
  "title": "OpenRBus",
  "config": {
    "step": {
      "user": {
        "title": "Select an ESPHome gateway",
        "description": "Select the verified ESPHome proxy response and generation sensors.",
        "data": {
          "response_entity": "Raw response sensor",
          "generation_entity": "Read generation sensor"
        }
      }
    },
    "abort": {
      "already_configured": "This gateway is already configured.",
      "no_esphome_gateway": "No ESPHome OpenRBus gateway action is available.",
      "no_esphome_entities": "No ESPHome OpenRBus response and generation sensors were found."
    }
  }
}
